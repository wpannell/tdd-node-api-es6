describe('the fetch-location canary test', () => {
  it('shows the infrastructure works', () => {
    true.should.equal(true);
  });

  describe('fetch location should', () => {
    it('retrieve the latitude and longitude');
  });
});
